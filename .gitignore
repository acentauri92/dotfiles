# All dotfiles are added on a case by case basis using the git --force command
# Ex: git add -f .vimrc
# Ignore all files in this folder (Home)
*

# Exceptions: files we actually want tracked
!.gitignore
!README.md
!.tmux.conf
!tmux-git-status.sh
!.gdbinit
!.bashrc
!ranger/
!ranger/rc.conf